instance/
__pycache__/
*.pyc
*.pyo
venv/
.env
*.db
.pytest_cache/
htmlcov/
.coverage
dist/
build/
*.egg-info/
.vscode/
.idea/
# Kaggle downloaded datasets (can be hundreds of MB)
~/.cache/kagglehub/
.kaggle/
# Claude Code internal state
.claude/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Sensitive local files — never commit
android.keystore
*.keystore
*.jks

# Claude Code search helper (local only)
.ignore
